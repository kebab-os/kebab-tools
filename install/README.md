# Installation

Use this directory for install scripts.

---

## `client-shell-install.sh`

Install kebab-tools client shell. Creates `kt` directory in root (`~`), and uses curl to create the file in the directory.
